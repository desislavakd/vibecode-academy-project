<?php

use Illuminate\Support\Facades\Route;

/*
|--------------------------------------------------------------------------
| API Routes (token-based / future use)
|--------------------------------------------------------------------------
| Routes here use the `api` middleware group and are prefixed with /api.
| Add token-authenticated endpoints here as the platform grows.
|
| Session-based SPA routes (used by the Next.js frontend) live in
| routes/web.php so the session middleware always runs for them.
*/
